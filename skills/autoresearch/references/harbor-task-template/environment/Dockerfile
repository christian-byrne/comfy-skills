# Task container — inherits from the base image built by the harness repo.
# Install any task-specific dependencies here.
FROM autoagent-base

# Example: install task-specific tools
# RUN pip install pandas openpyxl
# RUN apt-get update && apt-get install -y jq

WORKDIR /task
